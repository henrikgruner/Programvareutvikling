import styled from "styled-components/macro";
import { StyledLink } from "./StyledLink";

export const CancelButton = styled(StyledLink)`
  color: var(--font-color);
  font-weight: bold;
  font-size: 18px;
`;
